# ============================================================
# Vital_Traffic_Flow_in_Optics_Valley 依赖清单
# 安装：pip install -r requirements.txt
# ============================================================

# ---------- 深度学习 / 训练框架 ----------
# main.py 使用了 pl.Trainer.from_argparse_args 与 pl.callbacks.ModelCheckpoint(monitor=...)，
# 这些 API 属于 PyTorch Lightning 1.x 系列，装 2.x 会直接报错。
torch>=1.10,<2.0
pytorch-lightning>=1.5,<2.0

# ---------- 数据处理 ----------
pandas>=1.3
numpy>=1.21

# ---------- 可视化 ----------
# 图表中文标题依赖 SimHei 字体（Windows 自带）
matplotlib>=3.4

# ---------- 数据采集 ----------
requests>=2.25

# ---------- 可选：训练监控与超参寻优 ----------
# tensorboard>=2.8
# scikit-learn>=1.0
