
***

### 1. 模型搭建

基于 PyTorch Lightning 深度学习框架，本研究搭建了融合图卷积网络与门控循环单元的 T-GCN时空预测模型。模型的输入包含两部分：一是预处理后的光谷环岛交通速度特征序列，二是由 13 个核心节点构成的 $13 \times 13$ 阶有向邻接矩阵。根据路网特征，模型将历史观测时间步长（`seq_len`）设定为 18（即 1.5 小时），用于预测未来单个时间步的交通流状态。整体模型搭建分为空间特征提取、时间特征提取与训练目标函数配置三个核心模块。

**（1）空间依赖建模模块**
模型底层采用 GCN 提取路网节点的拓扑结构与空间依赖关系，刻画车流在相连路段间的扩散与汇聚效应。当特征矩阵与邻接矩阵输入模型时，其核心前向传播公式为：
$$f(A,X) = \sigma(D^{-\frac{1}{2}}\hat{A}D^{-\frac{1}{2}}XW)$$
其中，$\hat{A}=A+I$ 为加入自环的邻接矩阵，表示路段自身历史状态对当前的影响；$D$ 为度矩阵，用于对特征进行对称归一化以防止梯度爆炸；$X$ 为当前时间步的车速特征；$W$ 为模型在训练中需学习的权重参数；$\sigma(\cdot)$ 为非线性激活函数[cite: 9]。

**（2）时序演化建模模块**
在时间维度上，模型将 GCN 提取的空间特征嵌入到 GRU 单元中，将传统 GRU 内部的线性乘法操作替换为图卷积操作，构建时空联合门控机制。为避免在 13 节点的微循环网络中发生过拟合，本研究将隐藏层维度（`hidden_dim`）精简并设定为 32。模型内部通过更新门（$u_t$）与重置门（$r_t$）控制时空信息的记忆与遗忘，其完整的隐藏状态（$h_t$）迭代公式如下：
$$u_t = \sigma(W_u *_{\mathcal{G}} [X_t, h_{t-1}] + b_u)$$
$$r_t = \sigma(W_r *_{\mathcal{G}} [X_t, h_{t-1}] + b_r)$$
$$c_t = \tanh(W_c *_{\mathcal{G}} [X_t, (r_t \odot h_{t-1})] + b_c)$$
$$h_t = u_t \odot h_{t-1} + (1 - u_t) \odot c_t$$
其中，$*_{\mathcal{G}}$ 代表图卷积操作；$[X_t, h_{t-1}]$ 表示将当前路网输入与上一时刻隐藏状态在通道维度上进行拼接；$\odot$ 代表矩阵元素按位相乘。最终输出的 $h_t$ 实现了空间拓扑与时间序列的深度融合。

**（3）网络训练与损失函数配置**
在模型训练的超参数配置上，采用监督学习范式。基于实验寻优结果，网络批次大小（`batch_size`）设为 16，初始学习率（`learning_rate`）设为 0.001 以保障梯度下降的稳定性，最大训练迭代轮数（`max_epochs`）设定为 500 轮。

为指导网络权重的反向传播更新，采用均方误差（MSE）作为目标损失函数：
$$L = \frac{1}{T \times N} \sum_{t=1}^{T} \sum_{i=1}^{N} (y_{t,i} - \hat{y}_{t,i})^2$$
其中，$y_{t,i}$ 代表高德 API 采集的节点 $i$ 在时刻 $t$ 的真实车速，$\hat{y}_{t,i}$ 代表 T-GCN 模型输出的预测车速[cite: 9]。此外，模型代码中引入了 `ModelCheckpoint` 等回调机制（Callbacks），以监控验证集（Validation Set）的损失变化，确保在数百轮的迭代中动态保存最优的模型参数权重。