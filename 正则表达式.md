
---
在之前的代码中，使用的正则表达式是：**`r"从(.*)到(.*)"`** ,正是编写 `build_graph_v3.py` 时，用来把高德地图 API 返回的自然语言文本（比如“从珞喻路到南湖大道”）切割成可计算的 **起点（Origin）** 和 **终点（Destination）** 的那段关键代码

### 🎓 计划书直接替换文本

> “将该交通微循环网络定义为有向图 $G=(V, E)$，对应的邻接矩阵 $A \in \mathbb{R}^{N \times N}$ 定义为：当节点 $i$ 的车流可直接合法汇入节点 $j$ 时，矩阵元素 $A_{ij} = 1$；反之，若物理上无法直接连通，则 $A_{ij} = 0$。上述过程实现了物理路网向深度学习模型可识别的高维图结构的转化。
> 
> 在自动化构建过程中，本研究依托图论基础与有向图矩阵映射方法，**并辅以正则表达式 `r"从(.*)到(.*)"` 对高德 API 返回的非结构化‘行驶方向’文本进行语义解构。该表达式利用分组捕获机制（Capture Groups），精准提取出每一有向路段的物理起点（Origin）与终点（Destination），进而通过端点匹配算法（即前驱路段终点等于后继路段起点）自动生成连通边**，从而确保了抽象出的拓扑关系矩阵与实际复杂路网的高度契合。”

