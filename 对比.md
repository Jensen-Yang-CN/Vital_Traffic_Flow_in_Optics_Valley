
### 1. RMSE (Root Mean Square Error) —— 均方根误差
*   **含义**：这是衡量预测值与真实值（车速）之间差距的最常用指标。它先算误差的平方，再开方。
*   **直观理解**：它对“大误差”非常敏感。如果你的模型大部分预测很准，但偶尔有几次错得离谱（比如路段堵死你却预测畅通），RMSE 会迅速飙升。
*   **你的数据**：从 5.74 降到 4.09。意味着你的模型预测的平均误差大概在 **4 km/h** 左右。在城市交通预测中，4 km/h 的误差已经算非常优秀的结果了。

### 2. MAE (Mean Absolute Error) —— 平均绝对误差
*   **含义**：这是预测值与真实值之差的绝对值的平均数。
*   **直观理解**：它反映的是模型预测误差的“平均水平”，不像 RMSE 那样极端对待大误差。
*   **你的数据**：从 4.25 降到了 2.77。这意味着在 156 条路中，模型预测每一条路的速度，平均下来只比真实情况偏离了 **2.77 km/h**。

### 3. Accuracy (准确率) —— 核心评价指标
*   **含义**：在 T-GCN 的原始论文中，这个 Accuracy 通常不是分类里的那种对错，而是一个**回归准确率**。其计算公式通常是：$1 - \frac{\|Y - \hat{Y}\|_F}{\|Y\|_F}$（预测误差占总量的比例）。
*   **直观理解**：1.0 代表完美预测，0 代表完全瞎猜。
*   **你的数据**：**0.714**。在交通时空预测领域，考虑到交通流的随机性（比如突然的车祸、信号灯变化），准确率能达到 **70% 以上**就可以支撑出一篇高水平的国赛论文或 B 类以上学术论文了。

### 4. R2 (R-Squared) —— 决定系数
*   **含义**：衡量模型对数据波动的解释能力。越接近 1，说明模型越能抓到交通变化的规律。
*   **你的数据**：**0.846**。这意味着你的模型已经解释了交通数据中 **84.6%** 的波动。
*   **应用场景**：如果这个值很低（比如低于 0.5），说明你的模型只是在猜平均车速，并没有学到早晚高峰的变化规律。0.84 说明它已经深刻理解了“什么时候该堵，什么时候该顺”。

### 5. train_loss & val_loss —— 训练集与验证集损失
*   **含义**：这是模型在训练过程中尝试最小化的目标函数（通常是 MSE）。
*   **你的观察点**：
    *   **收敛性**：你的 Loss 从 0.01 左右降到了 0.001 左右，说明模型还在学习，没有“跑飞”。
    *   **过拟合检查**：如果 `train_loss` 一直降，但 `val_loss`（验证集损失）开始反弹上升，说明模型“背会了”深圳的数据，丧失了泛化能力。从你的截图看，`val_loss` 依然在缓慢下降或保持稳定，这说明模型非常健康。

### 6. ExplainedVar (Explained Variance Score) —— 解释方差分
*   **含义**：类似于 R2，衡量模型预测值与真实值之间的相关性。
*   **你的数据**：**0.846**，与 R2 几乎一致，进一步印证了模型的稳定性。

---

